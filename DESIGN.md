# KK-Car 全屏深色控制台

用户明确认可原深色图表区，要求全页采用这一风格，并保持全部内容不变。以下为当前设计规范，替代此前的浅色及深浅混合版本。

## 场景与主题

车载网络的所有者使用电脑集中查看状态，必要时用手机调整设置。按照用户的明确选择采用统一深色；以墨蓝灰为底，面板、图表、表单使用同一色系的不同明度，没有白色内容块。

- 页面底色：OKLCH 18–20%，面板 24%，辅助表面 29–32%，分隔线 35%。
- 正文 93%，辅助文字 73%；输入框文字、占位提示、下拉选项、确认框均适配深色。
- 下载浅蓝、上传薄荷绿、VPN 延迟浅紫、蜂窝信号青色。正常、警告、错误分别为绿、琥珀和柔红，始终带状态文字。
- 主操作按钮浅蓝深字，次操作采用深灰蓝浅字。提供悬停、焦点、按下、禁用和选中样式。
- 标题 12–20px，紧凑指标 10–11px，关键读数 14–26px。使用系统中文字体与等宽数字，不依赖远程字体。

## 布局与内容

- 全宽单页，无选项卡、分页或设置折叠。1800×863 窗口全部主要内容同屏；较窄窗口自然排列，手机保持完整内容和触控尺寸。
- 顶部为标题、状态、供电提醒和网络路径。
- 左侧为流量及 VPN 延迟图、4 组 32 项逐行对齐指标、设备与网络检查。
- 右侧为 VPN、网口、热点、维护设置；采样说明及上网棒刷新在网口下方。
- 统一 9px 面板圆角与细边框。保留现有标题、说明、数字、链接、按钮、表单和实时更新逻辑。

## 交互与验证

保持热点与网口的 2 分钟确认回退、暂停 VPN 确认、错误和过期提示。只修改 CSS，不修改网络配置、数据采集和页面内容。

所有资源本地加载。通过实际 Chrome 截图、页面尺寸、表单及标题清单检查深色一致性、内容保持和一屏显示。

## 指标色彩与联合图表

系统关键指标使用浅蓝和青色，上网接口使用蓝色，VPN 使用紫色，蜂窝使用青绿；常规时长和 MTU 用中性灰白，正常/警告/错误保留语义色。

VPN 延迟与 LTE RSRP 共享时间轴，延迟左轴 ms、紫色实线；信号右轴 dBm、青色虚线。信号轴固定 -150 至 -30 dBm，越接近 0 信号越强。指针详情展示信号均值及实际信号采样次数，避免将新启用信号误认为覆盖整个历史区间。

## AI 地区检查

网络检查新增 ChatGPT 边缘地区、Gemini 页面地区参考；分别显示国家码、HTTP 状态和来源。CN 红色、非 CN 绿色、未确定琥珀色，文字说明地区并不保证账号可用。桌面六项按三列两行排列，与网络四项分开计数，保持现有单屏布局。

## 飞书推送设置

首页保留一屏布局，右上角增加「飞书推送」入口。独立页面沿用深色墨蓝灰，桌面两列分别为事件/阈值和地址，手机自然单列，无选项卡。每种事件和每个地址独立开关；Webhook 不回显。状态区区分停用、未发送、飞书接收、失败和过期，保存/测试有忙碌与错误反馈。
