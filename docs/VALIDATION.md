# 验证与限制

## 2026-09-25 四页界面与 UPS 采样保护

- 网络总览、DJI 4G、UPS、飞书推送统一页面入口。首页继续保留单屏实时图表和详细数据；DJI 的左侧功能导航保留；UPS 把来电自启、采样与低电保护放在详细指标前，高级电池/电源操作放在后面；推送事件分组显示，保存后读回核对并在未保存时阻止误发测试。
- 本次只部署页面静态文件和 UPS 主控采样校验，没有更改 WAN、Wi-Fi、DHCP、VPN、推送开关或 UPS 寄存器设置。更新后从设备回读 WAN 在线、VPN 已连接、热点为 KK-Car；UPS 返回固件版本 10 和正常递增的运行计时。
- UPS 主控 42 字节原始帧增加范围与计时一致性校验，最多重试两次；异常时显示读取失败，不显示上一次或不可能的实时数字。13 项 ucode 断言覆盖低电策略、非法写入和异常帧；四个前端 JavaScript 文件通过 Node.js 语法检查。告警阈值和具体供电时间仍须以实际 UPS / 外部仪表校准。
- 重新登录后在真实 Chrome 页面检查四页桌面布局，并用 390×844 手机视口检查四页；均无横向溢出。网络总览在 1800×863 单屏显示图表和常用控制；DJI 总览新增 RSRP、RSRQ、SINR、RSSI 与实时工作小区参数，UPS 页的常用设置和详细读数正常渲染，飞书事件与地址分组正常。推送页勾选再撤销一个事件时，未保存提示相应出现并消失；本次未保存推送配置、未发送测试 Webhook，也未执行 UPS 电源操作。

## 2026-09-25 EP-0136 UPS 适配

- 按官方寄存器表核对 52Pi UPS Plus EP-0136 与树莓派 3B+ 兼容。只给 UPS 输入供电；启用 I²C 后实机发现主控 `0x17`、两颗传感器 `0x40/0x45` 和 RTC `0x68`。
- 连续读取主控寄存器，树莓派输出约 4.9–5.0V、电池端约 4.2V、USB-C 输入约 8.7–8.9V；电池温度在短时样本中约 47–51°C。树莓派 `get_throttled=0x0`，当前及此次启动没有记录欠压/降频。具体值随负载和充电状态变化。
- `kkups.status` 返回真实状态，并校验长度及数值范围；页面每 10 秒刷新，硬件失败时不沿用旧数据。浏览器实机打开页面，显示电量估计、输入约 8.9V、输出约 5.0V、电池温度约 51°C 和四个 I²C 地址；390px 手机宽度无横向溢出。安装页面后，`lan` 在线、KK-Car 5GHz/20MHz 热点仍在、蜂窝 WAN 和 VPN 没有被安装过程重启。
- 继续验收时，用户拔下 UPS 充电输入：页面显示输入接近 0V、由电池供电、估算电池电流为负，UPS 输出约 4.9V，树莓派 `get_throttled=0x0`。这是短时间转电池测试，不是续航测试。页面显示主控版本/序列号、两颗 INA219 的原始压降、估算电流/功率、RTC、计时、倒计时与所有可用配置项；两颗 INA219 的校准寄存器仍为 0，估算值尚未外部校准。
- 在真实设备上将采样周期从 2 分钟设为 3 分钟，读回为 3，再恢复到 2 并读回；RTC 从停振状态手动同步系统 UTC 时间，读回为有效。非法寄存器、无效布尔值和过低保护电压被拒绝。低电监控进程运行，策略默认关闭，读取状态为 disabled；未触发供电操作。页面桌面加载及 390px 手机视口检查通过，手机无横向溢出。
- 真实低电触发、UPS 安全关机/重启、恢复出厂、外部电源恢复后的自启、长时温度稳定性尚未验收。电量百分比需要完整充放电校准，不作为剩余续航证明。其他高级电池参数未写入实机。

基线时间：2026-09-19。以下为项目开发过程中已取得的验证记录，不能推断所有未来环境都相同。

## 已在真实设备验证

- F30A Pro USB 上网、5 GHz / 20 MHz 热点、LuCI 登录与权限。
- 国内、国外、公司内网、DNS 的实际访问；VPN 停止后国外业务不直出，恢复后可重新访问。
- 页面热点及网口控制的持久化读回；真实 LAN → WAN → LAN 操作，未接有线上级时保留 4G。
- IPv6 内核开关、地址和路由检查；接口重载后仍关闭。
- ADB 断开后重新连接、状态缓存过期处理。
- Ping 与信号历史采集、保存、读取；1 小时 / 30 天图表。
- ChatGPT 边缘地区与 Gemini 页面地区各返回 JP，HTTP 200；这只是当时的网络地区观测。
- Chrome 1800×863 一屏显示；已有手机适配验证记录。

## 隔离测试

`kk-car-ui/tests/` 提供热点与网口事务、路由选网、Ping 解析、历史聚合及 AI 地区解析测试。路由器测试在临时目录运行隔离夹具，不替换真实网络配置；`uplink-routing.py` 需要具备网络命名空间能力的 Linux 环境。

- 超时回退、确认保留、开机恢复、任务锁和非法输入。
- 真实 Linux 网络命名空间内的有线优先、防抖、失败回退、网段冲突及无出口阻断。
- 延迟/速率加权、丢包、重启恢复、损坏尾行、旧新信号格式兼容与重复/过期信号排除。
- 30 天 43201 分钟夹具汇总；不是等候 30 天实地记录。
- AI 地区 20 项测试：CN、非 CN、未知、403/429、超时、超大响应、失效格式、页面语言与无关国家文本误判。

现有测试脚本默认从仓库根目录的 `work/private/pi-admin` 与 `work/pi-reflash-known-hosts` 获取本地 SSH 凭据。它们故意不在仓库中；运行前自行准备自己的授权密钥或修改测试连接配置。测试路径仍以 `kk-car-ui/` 为项目子目录。

## 尚未验收或已知限制

- 没有长时间行车、跨基站、多个运营商或多 USB 上网棒切换验收。
- 真实有线 WAN 下的 IKEv2 尚未验证，模拟测试不能代替实机验收。
- 没有完成空白 SD 卡一键重建或整机冷启动验收。
- 曾持续检测到欠压和降频，网络检查通过不能证明供电已恢复正常。
- 不保证网络切换时 TCP 会话无中断，也不叠加多线路带宽。
- 5 GHz / 2.4 GHz 不能同时运行；页面的固定信道策略依赖当前设备环境。
- Gemini 页面内部字段可能改变；地区未知不代表 CN，非 CN 也不代表账号可用。
- 中国 IP 集合为静态快照，更新原始网段时需同步重建 nftables 集合；域名分流数据与设备配置需另行恢复。
- 本版本为单设备定制，接口名、内网目标与部分服务端地址仍需迁移适配。

## 2026-09-20 飞书推送迭代

- 36 项路由器临时目录隔离断言：VPN 防抖、延迟持续/回落区间、丢包、弱信号、温度、欠压、设备基线/接入/离线、出口变化、独立开关、冷却、过期样本、监控中断、地址校验、Webhook 不回显。
- 全工作器隔离测试（模拟 HTTP，不真实发送）：业务码成功/失败、失败留队列、测试请求与配置版本竞态、总开关、正常关机与普通服务重启。
- 实机测试消息取得 HTTP 200、飞书 code 0，待发队列归零。
- 页面关闭总开关后保存，后端回读一致、测试被拒绝、原地址保留；再恢复启用。
- 桌面实际页面检查；390×844 手机断点没有水平溢出、地址输入为空且不回显密钥。
- 未为验证而真实中断 VPN、切换出口或断电/重启；这些事件的判定依赖隔离测试，长时间移动网络与真实关机发送尚未做耐久验收。

运行 `python3 kk-car-ui/tests/notifications.py`，需要本地授权 SSH 配置。测试不会调用真实机器人或改动生产通知状态。

## 2026-09-20 网络恢复后的推送复测

- 在网络恢复后，逐一比对设备上的推送服务、工作器、配置校验和事件引擎，均与仓库工作版本一致；重新运行 36 项隔离断言、编译和工作器集成测试，全部通过。
- 先保持开机启动关闭，设置临时回退，再分阶段启动、暂停和恢复服务。两段运行观察合计约三分钟，Wi-Fi 管理连接、LAN 状态、DHCP 服务和 VPN 均正常，未复现网络失联。
- 首段运行采样的整机 CPU 约 3%–7%，可用内存约 787–794 MiB；这是整机采样，不是对推送进程独立资源消耗的测量。
- 恢复后的真实测试通知取得 HTTP 200、飞书业务码 0，待发队列为零；服务运行状态和开机启动项均已回读确认。
- 推送未启动时、暂停后以及运行中均观察到当前欠压/降频标志。供电问题独立存在；本次没有证据证明推送造成此前断网，也不能据此确定此前断网的唯一原因。
- 本轮没有再次重启、拔电或进行长期行车测试，开机启动项启用不等于已经完成冷启动耐久验收。有线口保留 LAN，但复测时没有接线，未重新验收有线 DHCP。

磁盘镜像、原始日志、运行采样、实际配置和临时维修脚本仅保存在本地私密目录，不属于公开仓库内容。可写分区曾完成离线日志恢复；修复成功与网络恢复的先后关系不能单独证明故障根因。

## 2026-09-20 HDMI 状态屏初版

- 在设备上通过 ucode 编译和 init 脚本语法检查。使用真实 `kkcar.status` 数据生成 656×416 BGRA 画面，读取并检查图像；修正整数除法导致的内存百分比错误。
- 实际 framebuffer 写入成功，服务运行且帧计数递增；第二轮取得 CPU、VPN 延迟、LTE RSRP、温度和 Wi-Fi 客户端数。单轮采集加绘制约 0.51 秒，此数据来自低分辨率模式，不代表 720p 性能。
- `vcgencmd display_power 1` 成功后，用户仍反馈物理显示器无信号；不能以程序运行替代物理显示验收。
- 私密备份原启动文件后，设置强制 HDMI 720p 并执行一次重启。管理电脑随后自动连接公司网络，两次重新连接热点未成功；重启后的网络、720p 画面与开机服务尚待复测。
- 本版没有完成外接显示器验收、热插拔、长期运行及墨水屏适配。HDMI 初版代码已保存，尚不能宣称物理 HDMI 已点亮。

## 2026-09-21 Wi-Fi 恢复与 VPN 备用管理

- 无线故障时观察到认证成功、DHCPACK 后立刻断开，以及 AP 实际 BSSID 与硬件/生成配置不一致。显式采用硬件地址并仅重建无线后，原异常客户端连续连接超过 10 分钟；Mac 强制走 Wi-Fi 探测 5/5 成功。尚不能将全部历史断网归因于这一项。
- 防火墙规则经 fw4 检查并回读，只开放 ikecar 区域的 TCP 22/80/443 和 Ping；管理返回路由指向 ikecar/table 300。
- Mac 强制绑定公司 Wi-Fi，Pi ikecar 抓到双向流量；Ping 3/3、SSH 密钥认证并核对本机启动 ID、LuCI 登录后 HTTP 200 均成功。本地网线始终保留，没有重启整机或 network。
- `python3 kk-car-ui/tests/vpn-management.py` 使用临时模拟路由环境，验证重复执行、地址变化、关闭/断线清理、部分失败重试及保留其他规则；不需要 SSH 凭据。两份 shell 脚本语法检查通过，两个 ucode 安装器在目标设备执行成功。
- 当前分配的 VPN 地址仍可能变化；自动维护返回路由不等于服务端固定地址。未验证新的冷启动、长期行车或 VPN 断开后的反向管理。

详见 [网络恢复与限制](NETWORK-RECOVERY.md)。

## 2026-09-21 HDMI 恢复及内存显示修正

- 开始时显示服务处于 inactive 且未启用开机启动；HDMI 电源打开、framebuffer 为 1280×720/32bit，程序模拟生成成功。恢复后，用户确认物理显示器可见且时间持续变化。
- 修复内存整数除法造成的 100% 假值；在设备 ucode 环境完成 9 个隔离算例，涵盖正常比例、可用内存为零、缺失时回退、全空闲、超范围钳制及缺失/无效数据。
- 设备编译通过，真实 framebuffer 回读确认内存约 15%、CPU、温度、LTE 信号、VPN 延迟与曲线正常显示。首帧 CPU/速率留空，第二帧开始产生采样。
- 模拟输出改写单独的诊断文件，不再覆盖实时画面状态。真实运行状态包含内存比例和上下行速率，便于核对。
- 恢复期间保留公司 Wi-Fi 经 VPN 的 SSH 管理，登录后的面板 HTTP 200。连续 6 轮观察中显示帧数递增、公司内网 Ping 6/6 成功，无线客户端持续连接；整机 CPU 约 4%–6%，单帧约 0.7–0.8 秒。没有为验证而重启整机或修改网络配置。开机启动项回读不等于冷启动/长期行车耐久验收。

## 2026-09-21 中文高密度 HDMI 布局

- 读取真实显示器 EDID 并验证两个块校验和，确认支持 CEA 16 / 1080p60。新增本地字体子集，无需安装图形桌面或浏览器。
- 1080p 模拟图像逐项检查中文、数字、双色曲线、32 项详情、在线设备与无历史检查结果状态。修正 ucode 不支持动态格式精度、UTF-8 字节处理及跨函数引用顺序等预览测试中发现的问题。
- `tests/hdmi-display.py` 在设备 ucode 环境通过 18 项纯计算断言，包括内存、数字格式、中文拆分、字节/时长、过期及未来时间。测试不写设备网络或显示状态；可用环境变量选择已有 SSH 管理路径。
- 注入 120 点高频波动数据的隔离模拟，预热后的最末帧约 3.68 秒；这是图表压力夹具，不是实际流量。文字按整行合成减少 framebuffer 行拷贝。
- 高密度程序先在实际 720p 下运行，预热后采样约 0.47–0.48 秒/帧，VPN Ping 3/3 成功，三台无线设备持续授权连接。此时未重启设备，真实 1080p 仍待切换。

## 2026-09-21 真实 1080p 切换与重启回归

- 用户确认维护窗口后，将原 HDMI 自定义块的 mode/width/height 改为 16/1920/1080；保留其余启动参数并备份 720p 配置，仅执行一次受控重启。
- 重启后从公司网络重新取得 SSH 管理连接；读取到新的启动 ID，实际 framebuffer 为 1920×1080、32 位 BGRA，显示服务自动运行，真实帧数持续增加。实机 framebuffer 回读显示中文高密度布局完整。
- IKEv2 重连分配了不同的虚拟地址；优先级 9980 的返回规则自动更新为新地址。公司侧重新登录 LuCI 后 HTTP 200，说明备用管理并未依赖旧地址。
- 公司服务 Ping 5/5 成功，无线客户端 Ping 2/2、4G 直连 Ping 2/2、国际 HTTPS 返回 204。原生 5 GHz/20 MHz 热点的实际地址仍与硬件地址一致，已授权无线客户端重新连接。
- 显示稳态样本约 0.68–0.70 秒/帧，整机 CPU 约 4%–7%，不是显示进程的独立 CPU 测量。
- 本次为一次软件重启回归，没有完成反复断电、行车、跨基站或长期耐久测试。历史欠压标记仍存在，不能以本次成功认为供电问题已消失。

## 2026-09-21 每 10 分钟自动检查

- `tests/auto-check.py` 使用隔离虚拟时钟验证正常 600 秒间隔、任务忙碌后 15 秒重试、RPC 故障后 60 秒重试、系统校时不重复运行、开机最早 60 秒首次检测，共五项通过。
- 真实设备已启用并运行自动检查服务，首轮由服务自动发起且完成；国内出口、VPN 出口、公司服务、DNS 均成功，两项 AI 地区信号均为 JP / HTTP 200。地区结果不保证账号可用。
- 实机状态中的间隔为 600 秒，下次计划时间比本次开始时间晚 600 秒；VPN 和 1920×1080 HDMI 服务继续运行。此次只刷新管理后台并重启显示服务，未重启网络。
- 已验证脚本语法与 ucode 编译；没有为本次功能再重启设备，也未以长时间实测替代虚拟时间测试。自动启动已 enable，后续真实冷启动仍需观察。

## 2026-09-21 固定地址请求与回退

爱快 `/32` 单地址池实测导致 100% 丢包，已回退原 `/30`，回读确认保存。树莓派持久化指定请求 `10.8.250.1`，重连后实际取得 `.1`，连续探测 3/3 响应、丢包 0%。随后六项诊断均获得成功结果，两项地区为 JP；Mac 切回公司网络后，经 `.1` 成功 SSH 登录，并核对优先级 9980 的返回规则使用该地址。此次未重启 LAN、Wi-Fi 或整机，也未将“指定请求”描述为服务端绝对静态绑定；步骤见 [网络恢复](NETWORK-RECOVERY.md#固定请求-vpn-管理地址)。地址变化推送改动未部署，原通知服务保持不变。


## 2026-09-23 DJI 一代 QMI 适配

- 原模块固件保留，USB 标识调整后模块重启、原生 option/qmi_wwan 自动识别，SIM 注册中国电信 LTE 并获得 IPv4。
- 蜂窝直连 Ping 3/3、国内 HTTP 200；逻辑有线 WAN 停用后自动切至 DJI，VPN 公司目标 3/3、国际 HTTPS 204。恢复有线后重新优先，VPN HTTPS 204。Wi-Fi 管理保持可用，DHCP 客户端仍在。
- 新装 QMI 后运行中的 netifd 未加载协议，首次进行了网络服务重启并配自动回退；未重启整机。首次验证超时触发回退，重新应用单 WAN 配置后接通。
- 路由与解析隔离测试覆盖动态 QMI 子接口、无网关 /32、同类出口换设备/地址、旧路由清理、暂停 VPN 保护、非法接口名、F30A 兼容、未知状态和采集超时；完整 nft 候选检查通过。历史测试验证换网卡时不制造速率峰值，HDMI 18 项辅助计算测试通过。
- QMI 数据未知时保留 null；设备可读、SIM 就绪、已连接数据会话、真实访问分别判定。
- 仍报告当前欠压；没有完成冷启动、物理 USB 重插、长期行车/跨基站和弱信号恢复耐久验收。GNSS、短信、eSIM 未启用或验收。详情见 [DJI QMI](DJI-QMI.md)。

最终有线优先状态下六项诊断通过：国内、VPN、公司、DNS 正常，ChatGPT/Gemini 地区信号均为 JP。浏览器核验最新 QMI 信号字段与未知值显示；HDMI 状态持续更新。地区结果仅描述此次探测，不保证账号服务可用。

## 2026-09-24 DJI 独立控制页

- `kkdji.uc` 与 `dji-sms.uc` 在设备 ucode 编译通过，两个脚本完成 shell 语法检查；菜单/ACL JSON 校验通过。短信模拟串口测试覆盖 23/23 满仓、目录不返回正文、单条中文读取、中文 PDU 发送、单条删除、输入拒绝、共享串口锁和异常遗留回复清理；这些是模拟能力验证，不代表实际短信已送达。
- 首次部署时，实机 AT 只读查询得到原模块固件版本、LTE Band 1、基带温度、SIM 已解锁、同频邻区 1 个及 GNSS 关闭状态；仅输出邻区数量和最强 RSRP，不保存小区标识。短信只读 `AT+CPMS?` 显示 ME 23/23 已满。当次未读取真实短信目录或正文，也未实际发送、删除短信。
- 设备上只增量安装脚本、rpcd 对象、ACL、菜单和页面资源，刷新 rpcd 与 LuCI 缓存；未重启 network、Wi-Fi、DHCP、VPN 或整机。浏览器在真实管理会话中打开独立页，核对 SIM、注册、会话、四项信号、频段、固件、温度、流量及短信满仓提示。手动「刷新状态」成功重新采样。
- 更新后原网络首页仍有 DJI 入口，VPN 隧道保持连接，蜂窝仍是当前出口，2 台 Wi-Fi 客户端在线。页面没有自动获取短信内容或坐标；GNSS 指令存在但天线和实机定位未验收，控制按钮关闭。当前只有蜂窝主用，未做会中断唯一出口的重连实测；也未做车载移动、弱信号及冷启动耐久验收。

## 2026-09-24 短信详情修复与 VoHive 操作对照

- 实机短信仓为 23/23。原页面用数字传送短信序号，而 RPC 参数声明为字符串；真实浏览器因此把正文显示为“空短信”。修复为字符串序号，并要求接口明确返回成功且包含正文，异常时显示错误，避免把失败误报为空短信。删除操作使用同样的字符串序号。
- 树莓派上的短信目录接口返回 23 条；逐条只读调用 23 条全部成功解析正文。验证过程只记录成功数量，未把号码、正文写入文档或仓库。浏览器实测打开不同短信的详情，并确认“清除当前内容”会移除页面中的正文。
- 短信中心改成可搜索的列表和独立详情区，移动端按单列排版；VoHive 的模组状态、短信、网络重连、通知、eSIM、代理与 VoWiFi 能力已在页面和文档中逐项标出当前边界。
- 本轮仅替换 LuCI 的 `dji.js` 和 `dji.css`，没有重启或重载网络、Wi-Fi、DHCP、VPN。未实际发送或删除短信；未运行 VoHive 二进制，未验证 eSIM、VoWiFi、USSD、定位或移动场景长期稳定性。

## 2026-09-24 长短信、加密归档、电话诊断与 SD 扩容

- 对标 VoHive、DJ 4G Hub、ModemDeck、Mac 4G 与 ModemManager 的官方项目说明，区分宿主机软件、VoWiFi 模拟外呼和真实 SIM 双向电话。没有将其他程序安装到当前 OpenWrt 设备，也没有改写 DJI 固件。
- 短信 PDU 新增 8/16 位拼接信息解析；合并条件包含发送方、引用号、总片数和日期，重复片号另起组，缺片显示不完整。模拟串口覆盖两种拼接格式、中文正文、存储满额、共享串口锁和输入拒绝。实机只读目录中 23 个模块存储槽形成 10 条会话，9 条完整、1 条缺片；未把真实号码或正文写入仓库。
- 原件确认存于模块 `ME`（23/23），不是 SIM 或 SD 卡。现存 23 个片段已导出为加密全量快照，解密校验数量为 23/10；密文复制到 SD 卡并比对 SHA-256。新服务对完整会话逐条 CMS 加密归档，路由器只持公钥证书；9 条完整历史会话已在 SD 卡生成密文，抽样在 Mac 用离线私钥解密并解析成功。缺片的现有片段在全量快照内。
- 飞书短信事件已在当前设备开启；独立服务显示启用、基线建立、零待发，历史短信未补发。真实新入站短信及飞书接收尚未发生，因此没有把转发描述为端到端验收。模块满仓时新短信可能无法进入，现有短信尚未删除。
- 电话只读检测：`AT+CLCC` 可响应，但 USB 语音位为关、IMS 配置为 0、USB 音频接口未枚举。页面开放能力检测，不开放拨号/接听；未测试运营商语音授权、双向音频或实际通话。
- SD 卡报告约 14.9GB，原 p2 104MB、overlay 85.9MB/可用约 62MB。离线保存并校验修改前前 180MiB 镜像后，原位扩展 p2 与 loop ext4；`df` 验证根可写区 14.0GB、可用约 13.3GB。未重启设备或网络；以后仍需观察冷启动和断电恢复。设备当前仍报告欠压历史及当前标志。

## 2026-09-24 SIM 短信仓与真实新短信

- 实机 `AT+CPMS=?` 证实 SIM `SM` 支持读取、写入、接收；查询显示 `SM` 原有 40/40、模块 `ME` 原有 23/23，`AT+CNMI?` 的入站模式为 1，即通知宿主读取已存短信。SIM 的 40 个片段形成 13 条完整会话。两处旧短信各自制作 CMS/AES-256 全量快照，Mac 使用离线私钥解密核验，密文复制到 SD 卡并比对 SHA-256。
- 用户同意清理旧短信后，按备份中的发件人、时间、分段信息和正文逐条核对并删除。删除导致模块序号重排，清理脚本改为跟踪稳定身份；期间刚收到的 1 条新短信未被删除。原 `ME` 旧 23 条和 SIM 旧 40 条已清理，最终 `SM` 0/40，`ME` 留有该过渡短信。
- 过渡短信由后台识别为新消息，SD 卡生成可解密的密文，飞书机器人返回成功且无待重试。未向仓库保存号码或正文。跨短信仓切换时，后台只把已有内容作为基线加密备份，避免历史短信被误发。
- 实机读回 `AT+CPMS?` 的读取、写入和接收三个位置均为 `SM`；管理页和脚本已按 SIM 存储显示与维护，模拟串口短信测试通过。服务和有线 WAN 仍运行。未做物理断电重启、脱离树莓派单独供电后的接收测试，因此不保证脱机后的存储设置保持或实际收到新短信；在树莓派重连后脚本会重新选择 `SM`。

## 2026-09-24 DJI 流量、短信自动加载与电话入口

- 实机向中国电信客服号发送一次查询短信并收到 4 段完整回复；私有解析只选择国内上网通用流量，排除同条回复中的定向套餐。公开文档、测试与仓库不保存原短信和实际套餐数字。
- 纯解析测试覆盖定向/通用混合、重复通用套餐拒绝、只有定向拒绝、MB/GB 换算与运营商标签不符拒绝；实机 worker 的今日/月度网卡字节持续增长，`traffic_status` 可读到最近校正时间、当地日历日期和定时设置。当前设为电信每日 09:00；首次导入当天已查过的短信后没有重复发送。
- 真实浏览器重新登录后，页面显示套餐估算、设备今日/月度流量、最近运营商报告与每天查询控制；刷新页面后无需按钮即可看到 4 段合并为一条的短信目录。电话只读复测仍为 USB 语音位关、IMS 0、USB 声卡未枚举，拨号/接听继续关闭。
- 部署只更新 DJI 后台脚本、RPC/ACL、LuCI 菜单与独立前端资源，并重启 rpcd 和 DJI 短信/流量服务；未重启或重载 network、Wi-Fi、DHCP、VPN。`lan` 与 `wan` 接口仍在线。每日自动短信与实际计费口径的长期准确性、移动/联通回复格式、真实通话和重启后的边界尚未实测。

## 2026-09-24 信号阈值、电话状态、定位与短信操作

- 独立页给 RSRP、RSRQ、SINR、RSSI 增加经验阈值和动态等级颜色；后端 SINR 改取 AT 采样，不把 QMI SNR 当成 SINR。浏览器实机回读四项数值、阈值及颜色；普通刷新可加载版本化的 `dji-console-v2` 资源。
- 模拟串口测试通过：短信长消息读取与合并、来电状态隐藏号码、排除 mode=1 数据会话、GNSS 启停及 fix 坐标/速度解析；前端 JavaScript、rpcd ucode、ACL 和菜单语法校验通过。真实模块 `AT+CLCC` 报告两个 mode=1 数据会话，过滤后页面显示电话线路空闲。电话能力仍为 USB 语音位关闭、IMS 0、无 USB 音频，未做拨号或接听。
- 真实模块短时启动 GNSS 后查询未获得卫星定位，随后停止并读回关闭；页面只在真实 fix 时显示位置和速度，不保存轨迹。实际天线、首次锁定和行车轨迹未验收。
- 短信目录自动刷新改为 30 秒，未读优先并按时间排序；“回复这条”只预填号码。浏览器验证现有四段长短信能合并打开，回复按钮可用，随后清空页面正文。未发送或删除短信。
- 部署只更新 DJI 的脚本、RPC、ACL、LuCI 菜单与版本化页面，不重启 `network`、Wi-Fi、DHCP、VPN 或整机。部署后 `lan`、`wan` 均在线，Wi-Fi AP 在运行，IKEv2 有一条已建立的 SA；GNSS 最终为关闭。树莓派仍报告当前欠压，移动与冷启动稳定性不能由本次页面验证推断。

## 2026-09-25 DJI 网页电话与来电提醒续测

- 树莓派在正常关机后的新一次启动中，QMI 蜂窝、VPN、HTTPS DJI 页面、模块临时语音驱动和音频网关均恢复。网页有通话控制按钮、麦克风权限和独立音频通道；驱动及 HTTPS 私钥不在公开仓库。
- 先前网页接听后 `AT+CLCC` 曾显示通话活动，但模块 USB 声卡采集失败且双方无声。随后用同一张电信手机卡从另一部手机拨入，只发送 `ATA`，不启动网页音频或模块 PCM 路由；通话仍在约 12 秒后中断。此对照没有提供有效语音媒体，不能排除媒体链路缺失导致断线；用户随后确认同一模块和 SIM 在 MaVo 上可双向通话。
- `AT+CEER` 为 `0,25`（参考表：网络释放，未给出原因）；`AT+QCFG="ims"` 为 `1,1`、`volte/disable` 为 0、IMS 数据上下文活动、活动 MBN 为 `VoLTE_OPNMKT_CT`。这不是 IMS 语音注册成功的证明。当前 LTE B3 的 SINR 约 −2 至 1 dB，树莓派报告 `0x50005` 当前欠压和降频；没有隔离对照，不作单因归因。
- 飞书通知队列记录真实来电事件并取得 HTTP 200、飞书业务码 0。通知采样改为约 5 秒，网页成功接听会留下无号码短期标记以避免误报未接；36 项通知状态/配置断言和工作器隔离集成测试通过，新增来电、未接与短通话已接分支。下一次真实来电仍需复核这项修正。
- `dji-sms.uc` 模拟串口测试通过，`call_diag` 设备只读回读通过，前端 JavaScript、ucode 与 shell 语法通过。此前部署及通知服务重启后，浏览器曾显示“未通过验收”说明，Wi-Fi 管理、QMI `wan`、VPN 和短信仍可访问；没有重启 network、Wi-Fi 或 VPN。当时尚未验证双向声音。

## 2026-09-25 DJI 网页电话双向通话短测

- 对照 [MaVo](https://github.com/moluncn/mavo) 和 [DJOneHubNative 的模块语音路径](https://github.com/cr-zhichen/DJOneHubNative/blob/main/docs/MODULE_RESEARCH.md)：均在 active 语音通话后启动 D4/UAC 模块路由，再由主机双向采集/播放。树莓派的模块侧运行时已相同，但 Go 网关没有持续静音补帧，网页断开后的进程清理也可能卡住；已修正并通过 Go 编译、`go test` 与 `go vet`。
- 真实来电定位到一个直接故障：OpenWrt 的 BusyBox `sleep` 拒绝 `0.1` 和 `0.5` 参数，`set -e` 使路由脚本在第一次等待时立即退出，接口便误判语音路由失败并主动挂断。将小数秒等待改为 ucode 后，脚本语法和实机准备检查通过。
- 修复后从另一部手机拨入，在 DJI 网页点击接听；`AT+CLCC` 显示活动语音通话，网页建立 HTTPS WebSocket，浏览器麦克风工作，模块 D4 与树莓派 Baiwang 声卡的输入/输出均运行。用户确认双方能听见；约 15–30 秒后主动挂断。网页回到空闲，声卡与模块语音路由停止、`arecord`/`aplay` 进程归零、网关仍接受新请求；QMI `wan` 和 KK-Car 热点在线，未重启 network、Wi-Fi 或 VPN。
- 此次只证明一次短时双向来电成功；第二通、出站拨号、长时及移动车载稳定性、飞书实时语音转接均未验收。

## 2026-09-25 DJI 开源界面对照与快捷入口

- 核对 DJOneHubNative、MaVo、DJOneHub 与 VoHive 社区实现的源码、项目截图或界面结构，形成独立[对照文档](DJI-BENCHMARK.md)。未运行这些上游程序，也未复制受限制许可的界面素材。
- DJI 页面只更新 JavaScript 和 CSS：首屏四个实时快捷入口可定位到电话、短信、信号、套餐；浏览器音频断开但 SIM 通话仍在进行时，可手动发起音频重连。未修改或重启 WAN、Wi-Fi、VPN、DHCP、模块固件。
- JavaScript 语法与公开发布清单验证通过。真实浏览器加载后，四个入口显示数据，电话入口能跳到电话卡片，短信列表自动更新；首次因浏览器缓存沿用旧 CSS，给新样式加版本号后再次加载，四卡布局正常。
- 音频重连按钮的可见性根据实时通话和浏览器连接状态决定；本次没有拨打第二通真实电话，因此重连成功率、出站与长时通话仍待实测。

## 2026-09-25 DJI 分区界面、电话本与小区参数

- DJI 独立页改为左侧 7 个功能入口、右侧工作区；桌面浏览器逐页检查总览、网络详情与电话，390px 手机宽度检查横向导航与单列布局。总览显示实时信号、套餐和电话摘要；电话页有拨号盘、联系人表单与通话记录。未进行新的真实拨入/拨出，因此电话本对实际来电的连续性仍待观察。
- `dji-phonebook.uc` 在隔离临时目录通过未接、拨出、联系人保存与删除状态测试；记录只写设备 0600 私有文件，飞书 worker 只推送无号码的状态文字。短信模拟串口测试与通知引擎 36 项断言、隔离 worker 集成测试通过。
- 实机只读 `AT+QENG="servingcell"` 已返回双工方式、MCC/MNC、Cell ID、TAC、PCI、EARFCN、上下行带宽，浏览器网络详情逐项显示；缺值仍按空值处理。真实小区标识没有写入仓库或本页文档。
- 部署后 `rpcd`、`kk-car-notify` 运行，DJI QMI 会话和蜂窝 WAN 在线，`br-lan` 仍为原管理地址、Wi-Fi AP 在线；未重启 `network`、Wi-Fi、DHCP 或 VPN。`node --check`、Shell 语法与设备 ucode 模块编译通过。
